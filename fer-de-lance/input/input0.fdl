let rec is_even = (lambda(n):
    if(n == 0): true
    else: is_odd(n - 1))
, is_odd = (lambda(n):
    if(n == 0): false
    else: is_even(n - 1))
in

# is_odd(5) && !(is_even(3)) &&
let x = 1 in
	let y = is_odd(10) in y